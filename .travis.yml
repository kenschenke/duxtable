language: node_js
script: npm run website-dist
local-dir: website/dist
before_install: if [[ `npm -v` != 5* ]]; then npm i -g npm@5; fi
deploy:
  provider: pages
  fqdn: www.duxtable.com
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  on:
    branch: master
