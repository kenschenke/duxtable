language: node_js
script: npm run website-dist
local-dir: website/dist
deploy:
  provider: pages
  fqdn: www.duxtable.com
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  on:
    branch: master
